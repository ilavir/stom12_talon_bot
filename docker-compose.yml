services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      - STATE_FILE_PATH=/app/data/state.json
    volumes:
      - talon-state:/app/data

volumes:
  talon-state:
